{{#if this.userWorkspaces}}
    <span class='font-size-22 font-weight-bold workspaces__title'>МОИ РАБОЧИЕ ПРОСТРАНСТВА</span>
    <div class='flex-column-centered flex-centered gap-20 workspaces__container'>
        {{#each userWorkspaces as |workspace|}}
            {{{workspace}}}
        {{/each}}
    </div>
{{else}}
    <div class='flex-row-centered'>
        <span class='font-size-16 font-weight-200 workspaces__title'>У вас пока что нет ни одного
            рабочего пространства.&nbsp</span>
        <button
            data-open='true'
            class='font-size-16 font-weight-200 btn btn-create-first-workspace'
        >Создать новое рабочее пространство</button>
    </div>
{{/if}}
